# Development files
.vscode-test/
.vscode/
*.vsix
*.vsixmanifest

# Source maps (not needed in production)
*.map
out/**/*.map

# TypeScript source files (compiled to JavaScript)
src/**/*.ts
!src/**/*.d.ts

# Exclude all node_modules anywhere
**/node_modules/

# Exclude all source files - only include compiled output
src/diagrams/
!out/diagrams/

# Specifically exclude webview source files
src/diagrams/webview/src/
src/diagrams/webview/node_modules/
src/diagrams/webview/package.json
src/diagrams/webview/package-lock.json
src/diagrams/webview/vite.config.ts
src/diagrams/webview/tsconfig.json
src/diagrams/webview/tsconfig.node.json

# Keep only the built webview files
!src/diagrams/webview/dist/

# Git files
.git/
.gitignore

# Documentation and examples (optional - can be included if needed)
examples/
sylang-alm-project/
test-project/
test-comma-relations.fml

# Log files
log.txt

# Development configuration
.cursorrules
sample.sylangconfig
sample.sylangextend

# IDE files
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db 